# Design System

## Intent

场景是安静办公室里长时间核对合同与表格：界面应像一张准确的工作台，而不是舞台。使用 restrained 色彩策略，主色只标记选择、主要动作和进行中状态。

## Tokens

所有颜色使用 OKLCH。背景为纯白，侧栏使用低色度钴蓝中性色；钴蓝 `oklch(0.56 0.16 258.4)` 为主色，琥珀仅用于警告。圆角以 8–14px 为主，控件动效 180ms，阴影不与装饰边框叠加。

## Typography

使用 Inter、`Noto Sans SC` 与系统无衬线字体。正文 14px/1.55，辅助文本 12px，页面标题 20px。数据与位置标识可用等宽字体。产品界面不使用展示字体和流体标题。

## Layout

桌面采用 280px / minmax(480px, 1fr) / 340px 三栏。左栏管理来源，中栏承载对话与任务输入，右栏展示执行与产物。窄屏先折叠右栏，移动端改为单列分段导航。

## Components

按钮、输入、状态标签、文件行、事件行和引用使用统一的 10px 圆角语言。加载使用骨架或行内进度；空状态必须讲清第一步；错误包含错误码、可理解说明与重试动作。

## Motion

仅为状态变化提供 150–220ms ease-out 动效。遵守 `prefers-reduced-motion`，无页面入场编舞。
